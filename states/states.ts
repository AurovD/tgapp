export type PendingState = {
    step: "Pending"
}

export type ReleasedState = {
    step: "Released"
}

export type Item = {
    releaseDate: Date;
    description: string;
}


export type User = {
    
}